version: 0.1

#environment_variables:
#  plaintext:
#    key: "value"
#    key: "value"

phases:
  install:
    commands:
      - chmod 700 $CODEBUILD_SRC_DIR/build/0.install.sh
      - $CODEBUILD_SRC_DIR/build/0.install.sh
  pre_build:
    commands:
      - chmod 700 $CODEBUILD_SRC_DIR/build/1.prebuild.sh
      - $CODEBUILD_SRC_DIR/build/1.prebuild.sh
  build:
    commands:
      - chmod 700 $CODEBUILD_SRC_DIR/build/2.build.sh
      - $CODEBUILD_SRC_DIR/build/2.build.sh
#  post_build:
#    commands:
      # TODO acceptance tests
      # TODO (In a different project) integration tests
      # TODO (In a different project?) performance tests
#artifacts:
#  files:
#    - .travis.yml # a useless but small file to satisfy the buildspec.yml requirement
